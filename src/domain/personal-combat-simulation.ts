import {buildEffectiveCombatStats,evaluatePredicate,evaluateValueExpression,type CombatContext,type RuntimeBaseStatBasis} from "./combat-context";
import {calculateActionDamage,calculateTuneBreakDamage,calculateTuneRuptureDamage,type DamageAmounts,type DamageTarget,type DamageType,type ScalingAttribute} from "./damage-engine";
import {resolveActiveEffects,type EffectAuditEntry} from "./effect-engine";
import type{ActionDefinitionV02,EffectDefinition,EffectModifier,FixedCritOverrideModifier,RuntimeStatModifier}from"./effect-models";
import{CombatEventQueue}from"./event-engine";
import type{CombatAction,FinalStats,MainEcho,Resonator,Sonata,UserBuild,Weapon}from"./models";
import{emptyCombatState,processEvent,timelineEvents,type ActiveRuntimeEffect,type CombatEvent,type CombatState,type StateDiagnostic,type StateTransition}from"./state-engine";
import type{TemporalTimeline}from"./temporal-engine";

export type DamageAttribution="direct"|"echo"|"follow-up"|"coordinated"|"summon"|"status"|"tune";
export interface PersonalDiagnostic{code:string;message:string;eventId?:string;actionId?:string;relevance?:"relevant-unsupported"|"modeled-unused"|"not-emitted-due-to-missing-context";}
export interface PersonalDamageAudit{timestamp:number;eventId:string;baseActionId:string;actionId:string;baseDamageType?:string;effectiveDamageType?:string;damageOwnerId:string;scalingOwnerId:string;actorId:string;triggeringActorId?:string;sourceEntityId?:string;targetId:string;attribution:DamageAttribution;panelStats:FinalStats;baseStatBasis:RuntimeBaseStatBasis;effectiveStats:FinalStats;runtimeStatAudit:ReturnType<typeof buildEffectiveCombatStats>["audit"];originalMotionValue:number;motionValueContributions:readonly{mode:string;value:number;effectId:string}[];effectiveMotionValue:number;activeEffectIds:readonly string[];effectAudit:readonly EffectAuditEntry[];damage:ReturnType<typeof calculateActionDamage>;sourceEffectId?:string;}
export interface Coverage{timelineSteps:number;relevantSupported:number;relevantUnsupported:number;modeledUnused:number;notEmittedDueToMissingContext:number;directDamageActions:number;derivedActions:number;activeEffects:number;triggeredEffects:number;}
export interface PersonalCombatResult{rotationDurationSeconds:number;personalDamage:DamageAmounts;personalDps:DamageAmounts;breakdown:Readonly<Record<DamageAttribution,DamageAmounts>>;perAction:Readonly<Record<string,DamageAmounts>>;perSource:Readonly<Record<string,DamageAmounts>>;audits:readonly PersonalDamageAudit[];eventLog:readonly CombatEvent[];stateTransitions:readonly StateTransition[];stateDiagnostics:readonly StateDiagnostic[];diagnostics:readonly PersonalDiagnostic[];unsupportedMechanics:readonly PersonalDiagnostic[];coverage:Coverage;partial:boolean;finalState:CombatState;}
export interface PersonalEffectLoadout{resonatorEffects?:readonly EffectDefinition[];sequenceEffects?:readonly EffectDefinition[];weapon?:Weapon;sonata?:Sonata;mainEcho?:MainEcho;extraEffects?:readonly EffectDefinition[];ownedEntityIds?:readonly string[];}
export interface PersonalCombatRequest{resonator:Resonator;build:UserBuild;timeline:TemporalTimeline;target:DamageTarget&{id?:string;tuneEnemyClass?:"1C"|"3C"|"4C"};resonanceMode?:string;scalingAttribute?:ScalingAttribute;baseStatBasis?:RuntimeBaseStatBasis;actions?:readonly(ActionDefinitionV02|CombatAction)[];loadout?:PersonalEffectLoadout;initialState?:CombatState;externalEvents?:readonly CombatEvent[];}

export function loadPersonalEffects(resonator:Resonator,sequence:number,loadout:PersonalEffectLoadout={}):{definitions:EffectDefinition[];audit:{effectId:string;source:string;accounting:string}[];diagnostics:PersonalDiagnostic[]}{const automatic=(resonator.combat?.effects??[]).flatMap(x=>x.structuredEffect?[x.structuredEffect]:[]);const equipment=[...(loadout.weapon?.effects??[]),...(loadout.sonata?.effects??[]),...(loadout.mainEcho?.effects??[])];const candidates=[...automatic,...(loadout.resonatorEffects??[]),...(loadout.sequenceEffects??[]),...equipment.flatMap(x=>x.structuredEffect?[x.structuredEffect]:[]),...(loadout.extraEffects??[])];const definitions:EffectDefinition[]=[],audit:{effectId:string;source:string;accounting:string}[]=[],diagnostics:PersonalDiagnostic[]=[];const seen=new Set<string>();for(const definition of candidates){if(seen.has(definition.id))continue;seen.add(definition.id);const rules=definition.rules.filter(r=>(r.requiredSequence??0)<=sequence);if(!rules.length&&!definition.triggers?.length&&!definition.statuses?.length)continue;const d={...definition,rules};definitions.push(d);for(const rule of rules)audit.push({effectId:d.id,source:d.source.type,accounting:rule.accounting});}return{definitions,audit,diagnostics};}

const zero=():DamageAmounts=>({nonCrit:0,crit:0,expected:0});const add=(a:DamageAmounts,b:DamageAmounts):DamageAmounts=>({nonCrit:a.nonCrit+b.nonCrit,crit:a.crit+b.crit,expected:a.expected+b.expected});
export function simulatePersonalCombat(req:PersonalCombatRequest):PersonalCombatResult{
 const targetId=req.target.id??"target",actions=new Map<string,ActionDefinitionV02>();for(const x of req.actions??req.resonator.combat?.actions??[]){const d="action"in x?x:{action:x};actions.set(d.action.id,d);}if(req.loadout?.mainEcho?.action)actions.set(req.loadout.mainEcho.action.id,{action:req.loadout.mainEcho.action});const loaded=loadPersonalEffects(req.resonator,req.build.sequence,req.loadout);
 const initial=req.initialState??emptyCombatState({[req.resonator.id]:{hp:req.build.finalStats.hp,maxHp:req.build.finalStats.hp,onField:true,resources:Object.fromEntries((req.resonator.combat?.resources??[]).map(r=>[r.id,{current:0,max:r.cap}]))}},[targetId]);let state:CombatState={...initial,activeEffects:[...initial.activeEffects,...loaded.definitions.filter(d=>!d.triggers?.length&&d.rules.some(r=>r.accounting==="runtime")).map(d=>({id:`initial:${d.id}:${req.resonator.id}`,definition:d,ownerId:req.resonator.id,activatedAt:0,stacks:d.lifecycle?.stacks?.initial??0,expiresAt:d.lifecycle?.duration.kind==="fixed"?d.lifecycle.duration.seconds:undefined}))]};
 const timeline=timelineEvents(req.timeline.entries,req.resonator.id,targetId),q=new CombatEventQueue(),diagnostics:PersonalDiagnostic[]=[...loaded.diagnostics];for(const e of[...timeline.events,...(req.externalEvents??[])])q.enqueue(e);const hitDependent=loaded.definitions.some(d=>(d.triggers??[]).some(t=>!t.externalContextRequired&&["action-hit","damage-dealt","critical-hit"].includes(t.event)));if(hitDependent&&timeline.missingHitTimings.length)diagnostics.push({code:"hit-timing-required",message:`Real hit timings are required by active triggers for: ${timeline.missingHitTimings.join(", ")}.`,relevance:"relevant-unsupported"});const criticalDependent=loaded.definitions.some(d=>(d.triggers??[]).some(t=>t.event==="critical-hit"));if(criticalDependent&&!(req.externalEvents??[]).some(e=>e.kind==="critical-hit"))diagnostics.push({code:"critical-hit-context-required",message:"Expected Crit does not generate probabilistic critical-hit events.",relevance:"not-emitted-due-to-missing-context"});for(const d of loaded.definitions)for(const t of d.triggers??[])if(t.externalContextRequired&&!(req.externalEvents??[]).some(e=>e.kind===t.event&&e.external))diagnostics.push({code:"external-trigger-context-required",message:`${t.id} has no external trigger event.`,relevance:"not-emitted-due-to-missing-context"});
 const audits:PersonalDamageAudit[]=[],transitions:StateTransition[]=[],stateDiagnostics:StateDiagnostic[]=[];const breakdown=Object.fromEntries((["direct","echo","follow-up","coordinated","summon","status","tune"]as const).map(k=>[k,zero()]))as Record<DamageAttribution,DamageAmounts>;const perAction:Record<string,DamageAmounts>={},perSource:Record<string,DamageAmounts>={};let triggered=0;
 const queueResult=q.drain(event=>{const derived:CombatEvent[]=[];const sr=processEvent(state,event,loaded.definitions,{panelStats:req.build.finalStats,resonanceMode:req.resonanceMode,element:req.resonator.element});state=sr.state;for(const emitted of sr.emittedEvents){const snapshot=emitted.payload?.snapshot as {stats?:string;stacks?:string}|undefined;let payload:Record<string,unknown>={...emitted.payload,damageOwnerId:emitted.payload?.damageOwnerId??req.resonator.id,scalingOwnerId:emitted.payload?.scalingOwnerId??req.resonator.id};if(snapshot?.stats==="trigger"||snapshot?.stacks==="trigger")payload={...payload,snapshotEffects:state.activeEffects.filter(e=>e.ownerId===req.resonator.id).map(cloneEffect)};derived.push({...emitted,ownerId:req.resonator.id,payload});}transitions.push(...sr.transitions);stateDiagnostics.push(...sr.diagnostics);diagnostics.push(...sr.diagnostics.map(d=>({code:d.code,message:d.message,eventId:d.eventId,relevance:"relevant-unsupported" as const})));triggered+=sr.transitions.filter(t=>t.kind==="effect-activated").length;
   const aggregate=event.kind==="action-start"&&!event.originEventId&&event.payload?.aggregateDamage===true;const realHit=event.kind==="action-hit"&&!event.external;const special=["tune-break","tune-rupture","fusion-burst"].includes(event.kind);if((!aggregate&&!realHit&&!special)||!event.actionId||event.external)return derived;if(event.payload?.statusId&&!state.targets[event.targetId]?.statuses[event.payload.statusId as string])return derived;const baseDef=actions.get(event.actionId);if(!baseDef){diagnostics.push({code:"action-not-found",message:`Action ${event.actionId} is not available.`,eventId:event.id,actionId:event.actionId,relevance:"relevant-unsupported"});return derived;}
   const live=state.activeEffects.filter(e=>e.ownerId===req.resonator.id),snapshotEffects=event.payload?.snapshotEffects as ActiveRuntimeEffect[]|undefined,snapshot=event.payload?.snapshot as {stats?:"trigger"|"hit";stacks?:"trigger"|"tick"}|undefined;const statSource=snapshot?.stats==="trigger"?snapshotEffects??live:live,stackSource=snapshot?.stacks==="trigger"?snapshotEffects??live:live;const active=statSource.map(effect=>{const stacks=stackSource.find(candidate=>candidate.id===effect.id);return stacks?{...effect,stacks:stacks.stacks,stackExpirations:stacks.stackExpirations}:effect;});let def=baseDef;let action=def.action;const preContext=contextFor(event,req,state,targetId,req.build.finalStats);for(const inst of active)for(const rule of inst.definition.rules)for(const modifier of rule.modifiers)if(modifier.kind==="action-replacement"&&modifier.actionId===action.id&&evaluatePredicate(modifier.condition,preContext).status==="matched"){const replacement=actions.get(modifier.replacementActionId);if(replacement){def=replacement;action=replacement.action;}else diagnostics.push({code:"replacement-action-not-found",message:modifier.replacementActionId,eventId:event.id,relevance:"relevant-unsupported"});}
   const talentLevel=req.build.skillLevels[action.talent as keyof typeof req.build.skillLevels];if(talentLevel!==undefined&&talentLevel!==action.level){const exact=def.multipliersByTalentLevel?.[talentLevel];if(!exact){diagnostics.push({code:"missing-exact-talent-multiplier",message:`No exact Lv${talentLevel} multiplier for ${action.id}.`,eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}action={...action,level:talentLevel as 10,multipliers:exact};}if(def.requirements){const prs=def.requirements.map(p=>evaluatePredicate(p,preContext));if(prs.some(p=>p.status==="unsupported")){diagnostics.push({code:"unresolved-condition",message:`Requirement unresolved for ${action.id}.`,eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}if(prs.some(p=>p.status==="ignored"))return derived;}else if(action.requiredState?.length){diagnostics.push({code:"unstructured-requirement",message:`Legacy requirements are not executable for ${action.id}.`,eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}
   let effectiveDamageType=action.damageType&&action.damageType!=="tuneRupture"?action.damageType:undefined;for(const inst of active)for(const rule of inst.definition.rules)for(const modifier of rule.modifiers)if(modifier.kind==="damage-type-replacement"&&evaluatePredicate(modifier.condition,preContext).status==="matched")effectiveDamageType=modifier.damageType;const ctx={...preContext,actionId:action.id,damageType:effectiveDamageType};const runtime:{stat:RuntimeStatModifier["stat"];mode:RuntimeStatModifier["mode"];value:number}[]=[],mv:{mode:string;value:number;effectId:string}[]=[];for(const inst of active)for(const rule of inst.definition.rules){const predicateResults=(rule.predicates??[]).map(p=>evaluatePredicate(p,ctx));if(rule.accounting!=="runtime"||predicateResults.some(p=>p.status!=="matched"))continue;for(const m of rule.modifiers){if(m.kind==="runtime-stat"){if(m.value.kind==="stat"&&m.value.view==="effective"){diagnostics.push({code:"runtime-stat-dependency-unsupported",message:`${inst.definition.id} depends on effective ${m.value.stat}.`,eventId:event.id,relevance:"relevant-unsupported"});continue;}const r=evaluateValueExpression(m.value,ctx,{stacks:inst.stacks,rank:inst.rank??req.build.weapon.rank});if(r.status==="supported")runtime.push({stat:m.stat,mode:m.mode,value:r.value!});else diagnostics.push({code:r.diagnostics[0]?.code??"invalid-expression",message:r.diagnostics[0]?.message??"Runtime expression unsupported.",eventId:event.id,relevance:"relevant-unsupported"});}else if(m.kind==="motion-value"){const r=evaluateValueExpression(m.value,ctx,{stacks:inst.stacks,rank:inst.rank??req.build.weapon.rank});if(r.status==="supported")mv.push({mode:m.mode,value:r.value!,effectId:inst.definition.id});}}}
   const effective=buildEffectiveCombatStats(req.build.finalStats,req.baseStatBasis??{},runtime);if(effective.status==="unsupported"){for(const d of effective.diagnostics)diagnostics.push({code:d.split(":")[0],message:d,eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}let damageAction=action;if(realHit&&event.hitIndex!==undefined&&!event.originEventId){const flattened=action.multipliers.flatMap(g=>Array.from({length:g.hits},()=>g.percent));const percent=flattened[event.hitIndex];if(percent===undefined){diagnostics.push({code:"hit-count-mismatch",message:action.id,eventId:event.id,relevance:"relevant-unsupported"});return derived;}damageAction={...action,multipliers:[{percent,hits:1}]};}const original=damageAction.multipliers.reduce((s,g)=>s+g.percent*g.hits,0),additive=mv.filter(x=>x.mode==="additive-percent").reduce((s,x)=>s+x.value,0),mult=mv.filter(x=>x.mode==="multiplier").reduce((s,x)=>s*x.value/100,1),factor=original===0?1:(original+additive)/original*mult;const effectiveAction=mv.length?{...damageAction,multipliers:damageAction.multipliers.map(g=>({...g,percent:g.percent*factor}))}:damageAction;
   const legacyInstances=active.map(i=>({...i,definition:{...i.definition,rules:i.definition.rules.map(r=>({...r,modifiers:r.modifiers.filter(m=>!["runtime-stat","motion-value","action-replacement","damage-type-replacement"].includes(m.kind)) as readonly(EffectModifier|FixedCritOverrideModifier)[]}))}}));const resolved=resolveActiveEffects(legacyInstances,{actorId:event.actorId,targetId,teamMemberIds:[req.resonator.id],element:req.resonator.element,damageType:effectiveDamageType as DamageType|undefined,resonanceMode:req.resonanceMode,actionId:action.id,combatContext:ctx});let damage:ReturnType<typeof calculateActionDamage>;
   if(event.kind==="tune-break"&&req.target.tuneEnemyClass)damage=calculateTuneBreakDamage({finalStats:effective.stats,attackerLevel:req.build.characterLevel,enemyClass:req.target.tuneEnemyClass,target:req.target,modifiers:resolved.tuneDamageModifiers}) as ReturnType<typeof calculateActionDamage>;else if((event.kind==="tune-rupture"||action.damageType==="tuneRupture")&&req.target.tuneEnemyClass)damage=calculateTuneRuptureDamage({action:effectiveAction,finalStats:effective.stats,attackerLevel:req.build.characterLevel,enemyClass:req.target.tuneEnemyClass,element:req.resonator.element,target:req.target,modifiers:resolved.tuneDamageModifiers,critOverride:resolved.overrides.fixedCrit}) as ReturnType<typeof calculateActionDamage>;else if(event.kind==="fusion-burst"){diagnostics.push({code:"formula-not-supported",message:"Fusion Burst damage formula is not verified.",eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}else damage=calculateActionDamage({action:effectiveAction,finalStats:effective.stats,attackerLevel:req.build.characterLevel,scalingAttribute:req.scalingAttribute??"attack",element:req.resonator.element,target:req.target,effectiveDamageType:effectiveDamageType as DamageType|undefined,modifiers:resolved.damageModifiers});if(damage.status==="unsupported"){diagnostics.push({code:damage.reason,message:damage.message,eventId:event.id,actionId:action.id,relevance:"relevant-unsupported"});return derived;}
   const payload=event.payload??{},attribution=(payload.attribution as DamageAttribution|undefined)??(action.talent==="echoSkill"?"echo":"direct"),damageOwner=(payload.damageOwnerId as string|undefined)??event.ownerId,scalingOwner=(payload.scalingOwnerId as string|undefined)??req.resonator.id;if(damageOwner!==req.resonator.id&&!(req.loadout?.ownedEntityIds??[]).includes(damageOwner))return derived;if(scalingOwner!==req.resonator.id){diagnostics.push({code:"foreign-scaling-owner-unsupported",message:scalingOwner,eventId:event.id,relevance:"relevant-unsupported"});return derived;}breakdown[attribution]=add(breakdown[attribution],damage.total);perAction[action.id]=add(perAction[action.id]??zero(),damage.total);const source=(payload.sourceEffectId as string|undefined)??(attribution==="direct"?req.resonator.id:attribution);perSource[source]=add(perSource[source]??zero(),damage.total);audits.push({timestamp:event.timestamp,eventId:event.id,baseActionId:baseDef.action.id,actionId:action.id,baseDamageType:baseDef.action.damageType,effectiveDamageType,damageOwnerId:damageOwner,scalingOwnerId:scalingOwner,actorId:event.actorId,triggeringActorId:event.triggeringActorId,sourceEntityId:event.sourceEntityId,targetId,attribution,panelStats:req.build.finalStats,baseStatBasis:req.baseStatBasis??{},effectiveStats:effective.stats,runtimeStatAudit:effective.audit,originalMotionValue:original,motionValueContributions:mv,effectiveMotionValue:effectiveAction.multipliers.reduce((s,g)=>s+g.percent*g.hits,0),activeEffectIds:active.map(e=>e.definition.id),effectAudit:resolved.audit,damage,sourceEffectId:payload.sourceEffectId as string|undefined});if(!aggregate)derived.push({id:`${event.id}:damage`,timestamp:event.timestamp,kind:"damage-dealt",ownerId:req.resonator.id,actorId:event.actorId,targetId:event.targetId,sourceId:action.id,actionId:action.id,originEventId:event.id,depth:(event.depth??0)+1,occurrence:`${event.occurrence??event.id}:damage`,payload:{damageOwnerId:damageOwner,scalingOwnerId:scalingOwner,triggerTimestamp:event.timestamp}});if(payload.statusId&&payload.consumeStacks){const status=state.targets[event.targetId]?.statuses[payload.statusId as string];if(status){const amount=payload.consumeStacks==="all"?status.stacks:Number(payload.consumeStacks);const statuses={...state.targets[event.targetId].statuses};const stacks=Math.max(0,status.stacks-amount);if(stacks===0)delete statuses[payload.statusId as string];else statuses[payload.statusId as string]={...status,stacks};state={...state,targets:{...state.targets,[event.targetId]:{...state.targets[event.targetId],statuses}}};}}return derived;});
 diagnostics.push(...queueResult.diagnostics.map(d=>({code:d.code,message:d.message,eventId:d.eventId,relevance:"relevant-unsupported" as const})));const personalDamage=Object.values(breakdown).reduce(add,zero()),duration=req.timeline.finalDurationSeconds,personalDps=duration>0?{nonCrit:personalDamage.nonCrit/duration,crit:personalDamage.crit/duration,expected:personalDamage.expected/duration}:zero(),unsupported=diagnostics.filter(d=>d.relevance==="relevant-unsupported"||d.relevance==="not-emitted-due-to-missing-context"),coverage:Coverage={timelineSteps:req.timeline.entries.length,relevantSupported:audits.length,relevantUnsupported:diagnostics.filter(d=>d.relevance==="relevant-unsupported").length,modeledUnused:diagnostics.filter(d=>d.relevance==="modeled-unused").length,notEmittedDueToMissingContext:diagnostics.filter(d=>d.relevance==="not-emitted-due-to-missing-context").length,directDamageActions:audits.filter(a=>a.attribution==="direct").length,derivedActions:audits.filter(a=>a.attribution!=="direct").length,activeEffects:state.activeEffects.length,triggeredEffects:triggered};return{rotationDurationSeconds:duration,personalDamage,personalDps,breakdown,perAction,perSource,audits,eventLog:queueResult.processed,stateTransitions:transitions,stateDiagnostics,diagnostics,unsupportedMechanics:unsupported,coverage,partial:unsupported.length>0||queueResult.partial,finalState:state};
}
function cloneEffect(e:ActiveRuntimeEffect):ActiveRuntimeEffect{return{...e,stackExpirations:e.stackExpirations?[...e.stackExpirations]:undefined};}
function contextFor(event:CombatEvent,req:PersonalCombatRequest,state:CombatState,targetId:string,panel:FinalStats):CombatContext{const actor=state.actors[event.actorId],owner=state.actors[event.ownerId],target=state.targets[targetId];return{timestamp:event.timestamp,actorId:event.actorId,ownerId:event.ownerId,targetId,sourceId:event.sourceId,sourceEntityId:event.sourceEntityId,triggeringActorId:event.triggeringActorId,damageOwnerId:event.payload?.damageOwnerId as string|undefined,scalingOwnerId:event.payload?.scalingOwnerId as string|undefined,actionId:event.actionId,eventKind:event.kind,eventSourceId:event.sourceId,eventTargetId:targetId,element:req.resonator.element,resonanceMode:req.resonanceMode,panelStats:panel,resources:owner?.resources,states:actor?.namedStates,form:actor?.form,onField:actor?.onField,actorHpRatio:actor?.hp!==undefined&&actor.maxHp?actor.hp/actor.maxHp:undefined,targetHpRatio:target?.hp!==undefined&&target.maxHp?target.hp/target.maxHp:undefined,targetStatuses:Object.fromEntries(Object.entries(target?.statuses??{}).map(([k,v])=>[k,v.stacks])),activeEffectIds:state.activeEffects.filter(e=>e.ownerId===event.ownerId).map(e=>e.definition.id)};}
