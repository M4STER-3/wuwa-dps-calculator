import { ArtworkNavigation } from "@/components/v2/artwork-navigation";
import { GlobalBackgroundStage } from "@/components/v2/global-background-stage";

export default function NavigationVisualTestPage() {
  return (
    <GlobalBackgroundStage>
      <ArtworkNavigation />
    </GlobalBackgroundStage>
  );
}
